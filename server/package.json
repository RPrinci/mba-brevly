{
  "name": "brev-ly-server",
  "version": "1.0.0",
  "type": "module",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "tsx watch --env-file .env src/infra/http/server.ts",
    "test": "dotenv -e .env.test -o -- vitest run",
    "test:watch": "dotenv -e .env.test -o -- vitest",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "tsx --env-file .env src/infra/db/migrate.ts",
    "db:push": "drizzle-kit push",
    "db:push:test": "dotenv -e .env.test -o -- drizzle-kit push",
    "db:studio": "drizzle-kit studio",
    "pretest": "pnpm run db:push:test",
    "pretest:watch": "pnpm run db:push:test",
    "build": "tsup-node"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@fastify/cors": "^11.2.0",
    "@fastify/swagger": "^9.6.1",
    "@fastify/swagger-ui": "^5.2.3",
    "drizzle-orm": "^0.45.1",
    "fastify": "^5.6.2",
    "fastify-type-provider-zod": "^6.1.0",
    "postgres": "^3.4.7",
    "zod": "^4.2.1"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.10",
    "@types/node": "^25.0.3",
    "dotenv-cli": "^11.0.0",
    "drizzle-kit": "^0.31.8",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vite-tsconfig-paths": "^6.0.3",
    "vitest": "^4.0.16"
  }
}
